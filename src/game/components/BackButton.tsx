import type { ButtonHTMLAttributes } from 'react'
export function BackButton(props: ButtonHTMLAttributes<HTMLButtonElement>) { return <button className="back-button" {...props}>← <span>QUAY LẠI</span></button> }
